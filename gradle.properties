org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
# Enabled now, deliberately, while the module count is small (2) — Gradle's own guidance is that
# compatibility issues are far cheaper to catch here than after Android/iOS plugins are added in
# Phase 3/4, and it becomes Gradle 10's default regardless.
org.gradle.configuration-cache=true

kotlin.code.style=official
